# Application model and embeddings (existing app knobs)
MODEL_CHOICE=gpt-4.1-mini
EMBEDDING_BACKEND=sentence
SENTENCE_MODEL=all-MiniLM-L6-v2
# OPENAI_EMBED_MODEL=text-embedding-3-large
# OPENAI_API_KEY=

# Vertex AI Vector Search configuration (Project-specific)
GCP_PROJECT_ID=
GCP_REGION=us-central1

# These are created by the ops/vertex/create_index.sh script
VECTOR_SEARCH_INDEX_ID=
VECTOR_SEARCH_ENDPOINT_ID=
# Optional: set if you know the deployed index ID (for direct neighbor queries)
# VECTOR_SEARCH_DEPLOYED_INDEX_ID=

# Isolation namespace for this project (REQUIRED)
NAMESPACE=project-b

# Optional: embedding dimensions override for index creation script
# EMBEDDING_DIMENSIONS=

# RAG collection name for local Chroma (not Vertex), if used elsewhere in the app
RAG_COLLECTION_NAME=docs
